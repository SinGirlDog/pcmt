<?php
// defined('IN_PHPCMS') or exit('Access Denied');
// defined('UNINSTALL') or exit('Access Denied');

// $where = array('name'=>'exam', 'parentid'=>29, 'm'=>'exam', 'c'=>'exam', 'a'=>'init', 'data'=>'', 'listorder'=>0, 'display'=>'1');

// $now_arr = $menu_db->get_one($where);

// $up_arr['display'] = 0;
// $nemu_db->update($up_arr,array('id'=>$now_arr['id']));
// $nemu_db->update($up_arr,array('parentid'=>$now_arr['id']));

// $language = array('exam'=>'模拟考试', 'setting'=>'考试配置', 'unreply'=>'暂时留空');


?>